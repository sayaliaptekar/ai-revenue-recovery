export default function Placeholder({title,description}) {
  return <div><div className="page-head"><div><h1>{title}</h1><p>{description}</p></div></div><div className="card center"><h2>Module ready for next phase</h2><p className="muted">We will connect this page to real analytics and customer data next.</p></div></div>
}
